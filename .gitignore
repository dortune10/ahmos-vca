# Dependencies
node_modules/

# Build output
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Test coverage
coverage/

# Internal planning docs stay local-only, not published to the public repo (PRD, decision
# log, implementation plans, execution reports) -- matches the pattern from the original
# AMHOS repo (github.com/dortune10/amhos), scoped to app code only.
docs/

# Backend (redundant with generic patterns above, kept explicit per plan)
backend/dist/

# Frontend (redundant with generic patterns above, kept explicit per plan)
frontend/.next/
.vercel
.env*
